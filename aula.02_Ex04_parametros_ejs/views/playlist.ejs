<!DOCTYPE html>
<html lang="pt-br   ">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vídeos</title>
  <link rel="stylesheet" href="/path/to/your/css/file.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
    <style>
        a {
    text-decoration: none !important; 
    color: black !important; 
    font-style: none;
  }

  .card-video {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
    margin: 10px;
    border-radius: 15px; 
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); 
    background-color: #fff; 
    text-align: center; 
    transition: box-shadow 0.3s ease; 
  }
  .card-playlist {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
    margin: 10px;
    border-radius: 15px; 
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); 
    background-color: #fff; 
    text-align: center; 
    transition: box-shadow 0.3s ease; 
  }
 
  .thumbnail{
    color: red ;
    font-size: 70px;
  }

  .listaplay{
    color: red ;
    font-size: 70px;
  }
    </style>
  <div class="container">
    <% if (playlist) { %>
      <h1>Seja bem-vindo à playlist: <%= playlist %></h1>
      <hr>
      <% if (video) { %>
        <h3>Vídeo Selecionado:</h3>
        <p>Não encontrou o que deseja? Pesquise na barra de navegação</p>
        <div class="card-video">
            <a href="/videos/<%= playlist %>/<%= video %>">
            <i class="bi bi-youtube thumbnail" style="font-size: 80px; color: red;"></i>
            <p class="link-video"><%= video %></p>
            </a>
        </div>
      <% } else { %>
        <h3>Alguns Vídeos da playlist:</h3>
        <p>Não encontrou o que deseja? Pesquise na barra de navegação</p>
        <% listaVideos.forEach((video) => { %>
            <div class="card-video">
                
                <a href="/videos/<%= playlist %>/<%= video %>">
                    <i class="bi bi-youtube thumbnail"></i>
                    <p class="link-video"><%= video %></p>
                </a>
            </div>
        <% }); %>
      <% } %>
    <% } else { %>
      <h3>Playlists recomendadas:</h3>
      <hr>
      <% listaPlaylists.forEach((playlist) => { %>
        <div class="card-playlist">
        <a href="/videos/<%= playlist %>">
          <i class="bi bi-list-stars listaplay"></i>
          <p class="link-playlist"><%= playlist %></p>
        </a>
    </div>

      <% }); %>
    <% } %>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
