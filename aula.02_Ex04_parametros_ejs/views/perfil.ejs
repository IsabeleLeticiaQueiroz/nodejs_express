<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Perfil</title>
    <link rel="stylesheet" href="/path/to/your/css/file.css" />
    <!-- Substitua pelo caminho real do seu CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <style>
      /* CSS inline para a imagem de perfil */
      .profile-image {
        width: 150px; /* Ajuste o tamanho conforme necessário */
        height: 150px; /* Ajuste o tamanho conforme necessário */
        border-radius: 50%;
        object-fit: cover; /* Garante que a imagem se encaixe no círculo */
      }

      .card-profile {
        padding: 20px;
        margin: 10px;
        border-radius: 15px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        background-color: #fff;
        transition: box-shadow 0.3s ease;
      }
    </style>
  </head>
  <body>
    <div class="container-fluid">
      <div class="text-center my-4">
        <h1>Meu Perfil</h1>
        <hr />
      </div>
      <% if (nome) { %>
      <div class="row text-center card-profile">
        <div class="col-md-6 ">
          <img
            src="https://cdn.pixabay.com/photo/2015/10/05/22/37/blank-profile-picture-973460_960_720.png"
            alt="Perfil"
            class="profile-image"
          />
          <p>Sua foto:</p>
        </div>
        <div class="col-md-6">
          <p><strong>Nome:</strong> <span id="nome"><%= nome %></span></p>
          <p><strong>Tel:</strong> <span id="telefone">13 984602834</span></p>
          <p><strong>Email:</strong> <span id="email"><%= nome %>123    @gmail.com</span></p>
        </div>
      </div>
     <% } %>

    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
